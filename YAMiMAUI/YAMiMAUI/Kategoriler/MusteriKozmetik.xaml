<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YAMiMAUI.Kategoriler.MusteriKozmetik"
             Title="Kozmetik Ürünleri"
             BackgroundImageSource="logo_arkaplan.png">
    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <CollectionView x:Name="UrunlerCollectionView"
                        ItemsSource="{Binding Urunler}"
                        SelectionMode="None"
                        Grid.Row="0">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="5" Padding="10" CornerRadius="10" BackgroundColor="White">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding KAdi}"
                                   FontSize="Medium"
                                   FontAttributes="Bold" />
                            <Label Grid.Row="1" Grid.Column="0"
                                   Text="{Binding KSatisF, StringFormat='Fiyat: {0:C}'}"
                                   FontSize="Small" />
                            <Label Grid.Row="2" Grid.Column="0"
                                   Text="{Binding KMiktari, StringFormat='Stok: {0} adet'}"
                                   FontSize="Small" />
                            <Button Grid.Row="0" Grid.RowSpan="3" Grid.Column="1"
                                    Text="Sepete Ekle"
                                    BackgroundColor="#28a745"
                                    TextColor="White"
                                    CornerRadius="5"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SepeteEkleCommand}"
                                    CommandParameter="{Binding KId}" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Sepetim Butonu -->
        <Grid Grid.Row="1" HorizontalOptions="End" VerticalOptions="Center">
            <Image Source="alisveris_sepet.png"
                   WidthRequest="50"
                   HeightRequest="50"
                   Aspect="AspectFit" />
            <Label Text="Sepetim"
                   FontAttributes="Bold"
                   TextColor="#520911"
                   FontSize="16"
                   Margin="5,58,0,0"
                   VerticalOptions="Center" />
            <Button WidthRequest="70"
                    HeightRequest="70"
                    BackgroundColor="Transparent"
                    CornerRadius="30"
                    Clicked="SepetimClicked"
                    HorizontalOptions="End" />
        </Grid>
    </Grid>
</ContentPage>